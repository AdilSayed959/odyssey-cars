const navbar = document.querySelector("nav"); window.addEventListener("scroll", () => navbar.classList.toggle("sticky", window.scrollY > 0)); const menu = document.querySelector(".menu"); const toggleMenu = () => menu.classList.toggle("active"); document.querySelector(".menu-btn").addEventListener("click", toggleMenu); document.querySelector(".close-btn").addEventListener("click", toggleMenu); document.querySelectorAll(".menu a").forEach((link) => link.addEventListener("click", toggleMenu)); function handleSubmit(event) { event.preventDefault(); const submitButton = document.getElementById('form-button'); const spinnerContainer = document.getElementById('spinner-container'); const spinner = document.getElementById("spinner"); submitButton.classList.add('hidden'); spinnerContainer.style.display = 'flex'; spinner.style.display = 'block'; const form = event.target; const data = new FormData(form); fetch(form.action, { method: 'POST', body: data }).then(response => { spinnerContainer.style.display = 'none'; if (response.ok) { window.location.href = 'ThankYou.html' } else { alert('There was a problem with the submission.'); submitButton.classList.remove('hidden') } }).catch(error => { spinnerContainer.style.display = 'none'; console.error('Error!', error.message); alert('There was a problem with the submission.'); submitButton.classList.remove('hidden') }) }
window.addEventListener('DOMContentLoaded', () => { const form = document.getElementById('contact-form'); form.addEventListener('submit', handleSubmit) })